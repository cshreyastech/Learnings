{
    "tasks": [
      {
        "type": "cppbuild",
        "label": "C/C++: g++-7 build active file",
        "command": "/usr/bin/c++",
        "args": [
          "-fdiagnostics-color=always",
          "-g",
          "${fileDirname}/Renderer.h",
          "${fileDirname}/VertexBuffer.h",
          "${fileDirname}/IndexBuffer.h",
          "${fileDirname}/Renderer.cpp",
          "${fileDirname}/VertexBuffer.cpp",
          "${fileDirname}/IndexBuffer.cpp",
          "${fileDirname}/Application.cpp",
          // "${fileDirname}/*.h",
          // "${fileDirname}/*.cpp",
          // "${file}",
          "-o",
          "${fileDirname}/${fileBasenameNoExtension}",
          "-lglfw",
          "-lGLU",
          "-lGL",
          "-lGLEW"
        ],
        "options": {
          "cwd": "${fileDirname}"
        },
        "problemMatcher": [
          "$gcc"
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
      "detail": "Task generated by Debugger."
    }
  ],
  "version": "2.0.0"
}